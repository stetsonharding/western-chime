import React from "react";

function CheckoutForm() {
  return (
    <div
      className="card-container"
      style={{
        color: "white",
      }}
    >
      This is the checkout form that still needs to be designed and implimented.
    </div>
  );
}

export default CheckoutForm;
